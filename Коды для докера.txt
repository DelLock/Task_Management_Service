Продакшн docker-compose -f docker-compose.prod.yml build

Девелопмент docker-compose -f docker-compose.dev.yml build


Запустить это вставить то же самое, толькое в конце up а не build



Установка зависимостей backend

cmd

cd backend
npm install

Установка зависимостей frontend

cmd

cd frontend
npm install